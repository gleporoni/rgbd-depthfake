# Required to make the "experiments" dir the default one for the output of the models
hydra:
  run:
    dir: ./experiments/${model.model_name}/${now:%Y-%m-%d}/${now:%H-%M-%S}

defaults:
  - _self_
  - run: train
  - model: rgb_xception
  - data: faceforensicsplusplus

project: 'depthfake'